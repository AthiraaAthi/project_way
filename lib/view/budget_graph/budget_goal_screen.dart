import 'package:flutter/material.dart';

class BudgetGoalScreen extends StatelessWidget {
  const BudgetGoalScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Budget Goal Screen"),
      ),
    );
  }
}
