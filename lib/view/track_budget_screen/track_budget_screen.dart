import 'package:flutter/material.dart';

class TrackBudgetScreen extends StatelessWidget {
  const TrackBudgetScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Track Budget Screen"),
      ),
    );
  }
}
